replicaCount: 1

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: wikijs.home
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - wikijs.home
      secretName: wikijs-tls
