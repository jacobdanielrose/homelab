replicaCount: 1

ingress:
  enabled: false

services:
  enabled: true

persistence:
  enabled: true
  size: 10Gi
  storageClass: longhorn

ollama:
  enabled: false

ollamaUrls:
  - http://ollama.ollama.svc.cluster.local:11434

pipelines:
  enabled: false

websocket:
  enabled: true
  manager: redis
  redis:
    enabled: true

extraEnvVarsSecret: open-webui-secrets

extraEnvVars:
  - name: ENABLE_INITIAL_ADMIN_SIGNUP
    value: "True"
  - name: WEBUI_ADMIN_EMAIL
    value: "admin@rostor.cloud"
  - name: WEBUI_ADMIN_NAME
    value: "Admin"
  - name: BYPASS_MODEL_ACCESS_CONTROL
    value: "True"
  - name: OLLAMA_API_KEY
    valueFrom:
      secretKeyRef:
        name: ollama-api-key
        key: OLLAMA_API_KEY
  - name: WEBUI_SEARXNG_BASE_URL
    value: "http://searxng.searxng.svc.cluster.local:8080"

sso:
  enabled: true
  enableSignup: true
  mergeAccountsByEmail: true
  oidc:
    enabled: true
    clientExistingSecret: open-webui-secrets
    clientIdExistingSecretKey: oidc-client-id
    clientExistingSecretKey: oidc-client-secret
    providerUrl: "https://authentik.rostor.cloud/application/o/open-webui/.well-known/openid-configuration"
    providerName: "Authentik"
    scopes: "openid email profile"
