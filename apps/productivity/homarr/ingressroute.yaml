apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homarr-local
  namespace: homarr
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`homarr.rostor.local`)
      services:
        - name: homarr
          port: 7575
  tls:
    secretName: homarr-tls