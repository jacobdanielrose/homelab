apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sure-cloud-tls
  namespace: sure
spec:
  secretName: sure-cloud-tls
  dnsNames:
    - sure.rostor.cloud
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
