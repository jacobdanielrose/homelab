apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: karakeep-tls
  namespace: karakeep
spec:
  secretName: karakeep-tls
  dnsNames:
    - karakeep.rostor.cloud
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
