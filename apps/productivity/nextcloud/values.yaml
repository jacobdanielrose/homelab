replicaCount: 1

nextcloud:
  host: nextcloud.home

ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - name: nextcloud.home
      path: /
      tls: []
  tls:
    - secretName: nextcloud-tls
      hosts:
        - nextcloud.home
