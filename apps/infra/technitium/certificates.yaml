apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: technitium-tls
  namespace: technitium
spec:
  secretName: technitium-tls
  dnsNames:
    - technitium.rostor.lab
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer