persistence:
  enabled: true
  volumeClaimSpec:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
    storageClassName: local-path

ingresses:
  http:
    enabled: true
    className: traefik
    hosts:
      - host: adguard.home
        paths:
          - path: /
            pathType: Prefix

services:
  dns:
    enabled: true
    type: LoadBalancer
