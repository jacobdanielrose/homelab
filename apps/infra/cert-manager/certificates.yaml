apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-tls
  namespace: traefik
spec:
  secretName: traefik-tls
  dnsNames:
    - traefik.home
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
