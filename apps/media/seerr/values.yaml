ingress:
  enabled: true
  ingressClassName: "traefik"
  hosts:
    - host: "seerr.rostor.cloud"
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - seerr.rostor.cloud
      secretName: seerr-cloud-tls

config:
  persistence:
    existingClaim: seerr-config-pvc
