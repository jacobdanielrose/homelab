apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seerr-cloud-tls
  namespace: jellyfin
spec:
  secretName: seerr-cloud-tls
  dnsNames:
    - seerr.rostor.cloud
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
