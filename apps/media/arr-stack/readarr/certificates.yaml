apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: readarr-tls
  namespace: readarr
spec:
  secretName: readarr-tls
  dnsNames:
    - readarr.rostor.home
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
