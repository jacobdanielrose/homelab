apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bazarr
  namespace: argocd
spec:
  project: default
  source:
    repoURL: oci://ghcr.io/bjw-s-labs/charts
    chart: bazarr
    targetRevision: 5.0.1
    #helm:
    #  valueFiles:
    #    - apps/media/arr-stack/bazarr/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: bazarr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
