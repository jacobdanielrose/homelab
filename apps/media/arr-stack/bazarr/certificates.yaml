apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazarr-tls
  namespace: bazarr
spec:
  secretName: bazarr-tls
  dnsNames:
    - bazarr.rostor.internal
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
