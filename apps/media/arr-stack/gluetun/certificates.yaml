apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: qbittorrent-tls
  namespace: gluetun
spec:
  secretName: qbittorrent-tls
  dnsNames:
    - downloads.rostor.internal
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
