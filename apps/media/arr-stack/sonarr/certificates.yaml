apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarr-tls
  namespace: sonarr
spec:
  secretName: sonarr-tls
  dnsNames:
    - sonarr.rostor.internal
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
