apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarr-tls
  namespace: sonarr
spec:
  secretName: sonarr-tls
  dnsNames:
    - sonarr.rostor.home
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
