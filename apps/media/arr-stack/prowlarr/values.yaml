ingress:
  main:
    enabled: true
    className: traefik
    hosts:
      - host: prowlarr.rostor.internal
        paths:
          - path: /
            pathType: Prefix
    annotations:
      cert-manager.io/cluster-issuer: selfsigned
    tls:
      - hosts:
          - prowlarr.rostor.internal
        secretName: prowlarr-tls

controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/linuxserver/prowlarr
          tag: latest
        env:
          TZ: Europe/Zurich
          PUID: "1000"
          PGID: "1000"
          PROWLARR__API_KEY:
            valueFrom:
              secretKeyRef:
                name: arr-api-key
                key: API_KEY

service:
  main:
    controller: main
    ports:
      http:
        port: 9696

persistence:
  config:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    storageClass: longhorn
    size: 1Gi
    globalMounts:
      - path: /config
  media:
    type: persistentVolumeClaim
    existingClaim: media-nfs-pvc
    globalMounts:
      - path: /data
