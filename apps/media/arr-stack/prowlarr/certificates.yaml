apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prowlarr-tls
  namespace: prowlarr
spec:
  secretName: prowlarr-tls
  dnsNames:
    - prowlarr.rostor.internal
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
