apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prowlarr-tls
  namespace: prowlarr
spec:
  secretName: prowlarr-tls
  dnsNames:
    - prowlarr.rostor.home
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
